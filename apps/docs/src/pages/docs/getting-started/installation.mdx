import { DocsLayout } from '@/layouts/docs-layout';
import { createPackageManagerTabs } from '@/utils/package-manager-tabs';

export const layout = DocsLayout;

export const config = {
  dependencies: {
    stylesheets: ['../../../components/code-tabs.css'],
    scripts: ['../../../components/code-tabs.script.tsx'],
  },
};

const ecosystemInstallTabs = createPackageManagerTabs(['@ecopages/radiant', '@ecopages/jsx']);
const signalsInstallTabs = createPackageManagerTabs(['@ecopages/signals']);

export const getMetadata = () => ({
  title: 'Docs | Installation',
  description: 'Install Radiant, JSX, and Signals for the workflow you want.',
})

# Installation

The most common setup is `@ecopages/radiant` plus `@ecopages/jsx`.

<radiant-code-tabs
  prop:label="Package managers"
  prop:tabs={ecosystemInstallTabs}
  prop:copyLabel="Copy install command"
  prop:initialTab="bun"
/>

## TypeScript Setup For JSX

```json
{
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "@ecopages/jsx"
  }
}
```
<br/>
Or enable it per file:

```tsx
/** @jsxImportSource @ecopages/jsx */
```

## Signals Standalone

If you only want the reactive core, install `@ecopages/signals` by itself:

<radiant-code-tabs
  prop:label="Signals package managers"
  prop:tabs={signalsInstallTabs}
  prop:copyLabel="Copy install command"
  prop:initialTab="bun"
/>

## Which Packages To Install

- Install `@ecopages/radiant` when you want custom elements, decorators, context, and host rendering behavior.
- Install `@ecopages/radiant` plus `@ecopages/jsx` when you want the component-first TSX workflow.
- Install `@ecopages/signals` when you want the standalone reactive layer.

