---
title: "Controller Context Visualizer"
description: "An interactive example showing one RadiantController provider sharing context with both controller and RadiantElement consumers."
group: Examples
order: 3
---

import { ControllerContextVisualizer } from '@/components/controller-context-visualizer';

export const config = {
	dependencies: {
		components: [ControllerContextVisualizer],
	},
};

# Controller Context Visualizer

An interactive context example built around authored HTML and `RadiantController` registration.

The provider lives on plain DOM marked with `data-controller`. From there, one shared context token reaches two different consumers:

- a `RadiantController` that updates authored markup imperatively with `@onContextUpdate`
- a nested `RadiantElement` that re-renders from a selected slice with `@contextSelector`

<ControllerContextVisualizer />

## What It Shows

- how `@controller`, `@provideContext`, `@onContextUpdate`, and `@contextSelector` each occupy different roles
- controller-provided context is available to descendants without requiring a custom-element provider host
- controller and element consumers can react to the same context updates while presenting those updates differently
- `startControllers(...)` can enhance authored markup while `RadiantElement` continues to render its own view normally

## Why This Example Matters

This is the main boundary that `RadiantController` opens up. You can keep server-authored or CMS-authored HTML as the source of truth for structure, attach behavior with controllers, and still share typed context with custom elements lower in the tree.

That makes mixed trees practical: controller-owned DOM for enhancement, custom elements where isolated rendering is useful, and one context graph across both.