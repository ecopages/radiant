---
title: Feed
description: Feeds structure a stream of articles or activity items with consistent header, content, and action regions.
category: Data display
---

import { meta as FeedMeta, Default } from '@/content/stories/feed';
import Canvas from '@/components/component-docs/canvas';
import Demo from '@/components/component-docs/demo';

export const config = {
	dependencies: {
		components: [Canvas, Demo],
		scripts: [
			'../../components/component-docs/demo.script.tsx',
			'../../components/component-docs/canvas.script.tsx',
			'../../components/component-docs/controls.script.tsx',
			'../../components/component-docs/code.script.tsx',
		],
	},
};


# Feed

<p class="docs-lede">Feeds structure a stream of articles or activity items with consistent header, content, and action regions.</p>

## Try it

<Demo of={Default} meta={FeedMeta} />

## Usage

Use `RuiFeedArticle` for each item. Compose bylines, metadata, and actions with the dedicated sub-components.

```tsx
import {
  RuiFeed,
  RuiFeedArticle,
  RuiFeedArticleHeader,
  RuiFeedArticleContent,
  RuiFeedByline,
} from '@ecopages/radiant-ui/feed';

<RuiFeed label="Activity">
  <RuiFeedArticle>
    <RuiFeedArticleHeader>
      <RuiFeedByline>Jane Cooper · 2 hours ago</RuiFeedByline>
    </RuiFeedArticleHeader>
    <RuiFeedArticleContent>Shipped order #4821.</RuiFeedArticleContent>
  </RuiFeedArticle>
</RuiFeed>
```

## Consistent article structure

<p>Keep header, content, and actions in the same order across items so users can scan the feed quickly.</p>

## Loading state

<p>Set `aria-busy` on the feed while fetching new items so assistive technologies announce the update.</p>

## Accessibility

- Label the feed region with the `label` prop or an external heading.
- Each article should have a discernible title or summary in the header.
- Action buttons inside articles need descriptive labels, not just icons.
