import type { PlaygroundScenario } from './types';

/** Declares a named playground example with optional fixed props and controls. */
export function defineScenario(scenario: PlaygroundScenario): PlaygroundScenario {
	return scenario;
}
