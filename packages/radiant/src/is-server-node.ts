/**
 * Node build of {@link ./is-server.ts}.
 *
 * @remarks
 * Resolved via the package `"node"` export condition for `@ecopages/radiant/is-server`,
 * including Node test runners with a browser-like DOM.
 */
export const isServer = true;
