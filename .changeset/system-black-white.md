---
'@ecopages/radiant-ui': patch
---

Move black and white to system tokens.

**@ecopages/radiant-ui**

- `--color-black` and `--color-white` now live in `tokens/system.css`, so every theme gets them without loading the Aurora pack.
