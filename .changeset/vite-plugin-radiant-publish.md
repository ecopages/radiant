---
'@ecopages/vite-plugin-radiant': minor
---

First publishable beta of the Radiant Vite 8 plugin with deterministic registry HMR, AST-based component discovery, and structural SSR document usage scanning.

- Requires Vite 8+ and documents dev-server full reload semantics for registry changes.
- Adds package tests for JSX config, metadata extraction, document usage discovery, and virtual module generation.
