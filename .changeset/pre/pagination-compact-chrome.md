---
'@ecopages/radiant-ui': patch
---

Apply the full narrow pagination chrome (icon-only previous / next, hidden page numbers) both below `40rem` and with `.rui-pagination--compact`, and add `previousText`, `previousLabel`, `nextText`, `nextLabel`, `pageLabel`, and `statusLabel` view props to localize the default copy.
